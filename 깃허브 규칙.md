# FinDA 팀 GitHub 작업 규칙

## 1. 기본 원칙

- `main` 브랜치는 항상 실행 가능한 안정 상태로 유지한다.
- `main` 브랜치에서 직접 작업하거나 직접 `push`하지 않는다.
- 모든 작업은 각자의 이니셜로 시작하는 브랜치에서 진행한다.
- 다른 팀원의 파일을 수정해야 할 때는 먼저 담당자와 공유한다.
- 분석 결과보다 **재현 가능한 코드와 작업 과정**을 우선한다.

## 2. 브랜치 규칙

브랜치 이름은 영문 소문자로 작성하고, 반드시 작업자 이니셜로 시작한다.

```text
<이니셜>/<작업명>
```

예시:

```text
jsw/application-eda
```

새 작업을 시작할 때는 최신 `main`에서 브랜치를 만든다.

```bash
git switch main
git pull origin main
git switch -c <이니셜>/<작업명>
```

- 한 브랜치에는 하나의 작업 목적만 담는다.
- 이미 병합된 브랜치는 재사용하지 않고 새 브랜치를 만든다.
- 다른 사람이 사용하는 브랜치에 임의로 커밋하거나 강제 푸시(`--force`)하지 않는다.
- 작업 기간이 길어지면 `main`의 최신 변경사항을 자신의 브랜치에 주기적으로 반영한다.

## 3. 분석 목적별 폴더 및 개인 파일 규칙

사람별 최상위 폴더를 만들지 않는다. `EDA/`, `Analysis/`처럼 **분석 단계와 목적별 폴더**를 만들고, 각 폴더 안에서 담당자별 파일을 분리한다.

```text
EDA/
├─ notebooks/
│  ├─ application_previous_jsw.ipynb
│  ├─ bureau_khj.ipynb
│  ├─ installments_lsy.ipynb
│  └─ pos_credit_card_pmk.ipynb
├─ src/
│  ├─ application_previous_features_jsw.py
│  ├─ bureau_features_khj.py
│  ├─ installments_features_lsy.py
│  └─ pos_credit_card_features_pmk.py
├─ data/
│  └─ derived/
├─ figures/
└─ reports/

Analysis/
├─ notebooks/
├─ src/
├─ data/
│  └─ derived/
├─ figures/
└─ reports/
```

개인 작업 파일은 다음 형식을 권장한다.

```text
<작업내용>_<이니셜>.<확장자>
```

- 개인 노트북, 스크립트, 중간 결과, 그래프와 보고서 초안은 해당 분석 목적 폴더 안에 저장한다.
- 각 담당자는 자신의 이니셜이 표시된 서로 다른 파일을 수정한다.
- 다른 팀원의 파일을 수정해야 할 때는 먼저 해당 담당자와 공유한다.
- 여러 사람이 하나의 공용 노트북이나 문서를 동시에 수정하지 않는다.
- 최종 통합 파일은 담당자를 정한 뒤 각자의 결과가 완료되었을 때 생성한다.
- 공통 함수로 확정된 코드는 팀 검토 후 공용 파일로 분리하며, 공용 파일의 수정 담당자를 지정한다.
- `최종`, `진짜최종`, `수정본2` 대신 작업 내용과 담당자를 알 수 있는 파일명을 사용한다.

## 4. 데이터 관리 규칙

- 원본 데이터는 수정, 삭제, 이름 변경, 덮어쓰기를 하지 않는다.
- 원본 데이터 폴더는 읽기 전용으로 취급한다.
- 전처리·집계·샘플링으로 만든 파생데이터는 해당 분석 목적의 `<분석목적>/data/derived/` 폴더에 저장한다.
- 파생데이터 파일명에도 담당자 이니셜을 붙여 생성자와 관련 코드를 확인할 수 있게 한다.
- 파생데이터를 생성한 코드도 함께 저장하여 원본 데이터로부터 다시 만들 수 있어야 한다.
- 코드에서는 개인 PC의 절대경로가 아닌 저장소 루트 기준 상대경로를 사용한다.
- 원본 데이터, 대용량 CSV·Parquet, 모델 파일, 임시 파일은 Git에 올리지 않고 `.gitignore`로 관리한다.
- 팀 공유가 필요한 데이터는 Git에 직접 올리지 말고 팀이 합의한 별도 공유 위치와 파일명을 문서에 기록한다.
- 작은 샘플 데이터가 꼭 필요하면 개인정보·민감정보 포함 여부와 용량을 확인한 뒤 팀 합의 후 추가한다.

## 5. 커밋 규칙

- 커밋은 가능한 한 작은 작업 단위로 나눈다.
- 커밋 메시지만 보고 변경 내용을 알 수 있도록 작성한다.
- 커밋 메시지 맨 앞에는 작업 날짜를 `MM.DD` 형식으로 작성한다.
- 월과 일은 두 자리로 표기하고 날짜 뒤에 한 칸을 띄운다.
- 코드 변경과 관계없는 임시 파일, 캐시, 대용량 출력은 커밋하지 않는다.

커밋 메시지 형식:

```text
<MM.DD> <변경 내용>
```

예시:

```text
07.26 EDA 기초 작업
07.26 bureau 고객 단위 집계 기능 추가
07.27 결측치 처리 오류 수정
07.28 EDA 결과 해석 추가
```

## 6. Pull Request(PR) 규칙

- 개인 브랜치의 작업이 끝나면 `main`으로 Pull Request를 생성한다.
- PR을 만들기 전에 최신 `main`을 반영하고 충돌 여부를 확인한다.
- PR은 필수 코드 리뷰보다 변경 기록과 충돌 확인을 위해 사용한다.
- 각자 다른 파일에서 진행하는 독립 분석 작업은 별도의 리뷰 승인을 기다리지 않아도 된다.
- 작성자가 아래 항목을 직접 확인한 뒤 자신의 PR을 바로 `main`에 병합할 수 있다.
  - 원본 데이터를 수정하지 않았는지 확인한다.
  - 다른 팀원의 파일을 임의로 수정하지 않았는지 확인한다.
  - 노트북 또는 코드가 정상적으로 실행되는지 확인한다.
  - 대용량 데이터, 임시 파일, 비밀정보가 포함되지 않았는지 확인한다.
  - 최신 `main`과 충돌이 없는지 확인한다.
- `README.md`, `.gitignore`, `requirements.txt`, 공통 함수, 통합 리포트 등 공용 파일을 수정할 때는 병합 전에 팀에 변경 내용을 공유한다.
- 최종 통합 단계에서는 파일 충돌뿐 아니라 각 담당 결과의 컬럼명, 저장 형식과 해석 기준도 함께 확인한다.
- 병합이 끝난 개인 작업 브랜치는 삭제한다.

권장 작업 흐름:

```text
개인 브랜치 작업
→ 개인 브랜치 push
→ main 대상 PR 생성
→ 충돌 및 실행 여부 본인 확인
→ 작성자가 바로 merge
→ 병합된 브랜치 삭제
```

PR 설명에는 다음 내용을 작성한다.

```text
1. 작업 목적
2. 주요 변경 파일
3. 실행 방법
4. 생성된 결과
5. 검증 내용
6. 확인이 필요한 사항
```

## 7. 코드와 분석 결과 규칙

- 노트북과 스크립트는 저장소 루트에서 실행할 수 있어야 한다.
- 난수를 사용하는 분석과 모델은 `random_state` 또는 seed를 고정한다.
- 새 라이브러리를 사용하면 `requirements.txt` 등 환경 설정 파일에 반영한다.
- 노트북은 커널 재시작 후 처음부터 끝까지 실행되는지 확인한다.
- 불필요하게 큰 셀 출력과 디버깅 로그는 정리한 뒤 커밋한다.
- 표와 그래프에는 제목, 축 이름, 단위, 표본 수 또는 분모를 표시한다.
- 분석 결과에는 사용 데이터, 전처리 기준, 결측 처리, 한계를 함께 기록한다.
- 관측된 상관관계를 인과관계나 실제 대출 승인 효과로 단정하지 않는다.

## 8. 보안 및 업로드 금지 파일

다음 항목은 GitHub에 커밋하지 않는다.

- API 키, 비밀번호, 토큰, 인증서
- `.env` 파일과 개인 환경 설정
- 개인정보 또는 외부 공개가 허용되지 않은 데이터
- 원본 데이터와 불필요한 대용량 파생데이터
- 캐시, 로그, 임시 파일, 가상환경 폴더

비밀정보를 실수로 커밋했다면 단순히 파일만 삭제하지 말고, 즉시 팀에 알린 뒤 해당 키나 토큰을 폐기하고 재발급한다.

## 9. 작업 전·후 체크리스트

### 작업 시작 전

- [ ] `main` 브랜치를 최신 상태로 업데이트했다.
- [ ] `<이니셜>/<작업명>` 형식의 새 브랜치를 만들었다.
- [ ] 작업 파일과 담당 범위를 팀에 공유했다.
- [ ] `EDA/`, `Analysis/` 등 담당 작업의 목적별 폴더에서 작업하고 있다.
- [ ] 개인 작업 파일명에 담당자 이니셜을 표시했다.

### PR 생성 전

- [ ] 원본 데이터를 수정하지 않았다.
- [ ] 파생데이터와 출력 파일이 해당 분석 목적 폴더에 저장되어 있다.
- [ ] 파생데이터와 개인 출력 파일명에 담당자 이니셜이 표시되어 있다.
- [ ] 절대경로, 비밀키, 대용량 파일이 포함되지 않았다.
- [ ] 코드 또는 노트북을 처음부터 실행해 확인했다.
- [ ] 불필요한 출력과 임시 파일을 정리했다.
- [ ] 모든 커밋 메시지가 `MM.DD 변경 내용` 형식으로 작성되어 있다.
- [ ] 커밋 메시지와 PR 설명만으로 변경 내용을 이해할 수 있다.

## 10. 팀에서 작업 시작 전에 추가로 정할 사항

- 팀원별 이니셜과 담당 분석 영역
- `EDA/`, `Analysis/` 등 분석 목적별 폴더 구조
- 공용 파일과 최종 통합 파일의 담당자
- 원본 데이터 위치와 파생데이터 공유 방법
- Python 버전과 공통 패키지 버전
- 개인 작업의 self-merge 방식과 공용 파일 변경 시 공유 방법
- 최종 노트북·리포트·발표자료의 담당자
- 파일 및 변수 이름 규칙
- 분석 결과와 모델 성능의 공통 검증 기준
